/**
 * [(lv.1)pass-or-fail.js]
 *
 * 점수(score)를 전달받아 다음을 수행하세요:
 * 1) score가 60 이상이면 "Pass"를,
 * 2) 그 외의 경우에는 "Fail"을 반환하세요.
 *
 * @param {number} score
 * @returns {string} "Pass" or "Fail"
 */

// TODO: passOrFail 함수를 작성하세요.
function passOrFail(score) {
  return score >= 60 ? "Pass" : "Fail";
}

// export를 수정하지 마세요.
export { passOrFail };
