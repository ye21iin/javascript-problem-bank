/**
 * [(lv.2)객체-메서드-Keys.js]
 *
 * getObjectKeys 함수에 대한 요구사항:
 * 1) obj 객체가 주어졌을 때, Object.keys(obj)를 사용해 객체가 가진 "모든 직접 소유 키"를 배열로 반환하세요.
 * 2) 예: obj = { name: "Alice", age: 25 } -> ["name", "age"]
 *
 * @param {object} obj
 * @returns {string[]}
 */
function getObjectKeys(obj) {
  // TODO
}

// export를 수정하지 마세요.
export { getObjectKeys };
