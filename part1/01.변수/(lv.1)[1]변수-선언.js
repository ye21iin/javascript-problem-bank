/**
 * Q1.1: 변수 선언
 * var x, let y, const z를 각각 선언하고, 아래와 같은 값을 할당하세요:
 *    - x: number (예: 42)
 *    - y: string (예: "Hello")
 *    - z: boolean (예: true)
 */

var x = 42;
let y = "Hello";
const z = true;

// export 를 수정하지 마세요.
export { x, y, z };
